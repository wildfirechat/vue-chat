export default class ForwardType {
    /**
     * 普通转发
     * @type {number}
     */
    static NORMAL = 0;
    /**
     * 逐条转发
     * @type {number}
     */
    static ONE_BY_ONE = 1;
    /**
     * 合并转发
     * @type {number}
     */
    static COMPOSITE = 2;
}
