import UserInfo from "@/wfc/model/userInfo";

export default class WfcHelper {

    /**
     *
     * @param {string | Object} user
     * @param {string} groupId
     */
    static userName(user, groupId) {
        if(user instanceof UserInfo){
            let userI
        }else {

        }
        return
    }
}
